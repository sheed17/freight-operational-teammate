"""Freight invoice reconciliation & document engine.

Phase 1 surface: config loading, confidence-scored extraction models, and the
PyMuPDF + Instructor vision extraction pipeline. Later phases (matching, state
machine, Slack HITL, bounded TMS agent) plug into this same chassis.
"""

__version__ = "0.1.0"
