# COMPATIBILITY MIRROR of pyproject.toml [project.dependencies] — pyproject is canonical.
# Bootstrap canonically with: python3 scripts/check_env.py && pip install -e ".[dev]"
# (U-HANDOFF-1B: this file previously drifted from pyproject and omitted websocket-client.)
pydantic>=2.7
instructor>=1.5
anthropic>=0.39
openai>=1.40
pymupdf>=1.24
pyyaml>=6.0
python-dotenv>=1.0
reportlab>=4.0
pillow>=10.0
