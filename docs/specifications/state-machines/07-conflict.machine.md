# Machine M7 — Conflict

*Registry: `registry.md`. Entity: `entities/10-conflict.md`. Lifecycle: Target Spec §12.7; ADR-007 §5.*

**1.** Conflict. **2.** Conflict. **3. Purpose.** Make incompatible claims/observations on one field **visible and blocking**; the mechanism by which Neyma *never silently chooses*. **4. Aggregate/txn.** Own aggregate; raising a Conflict + setting the field `conflicting` = one commit. **5. Owner.** ### **a named human, from `RAISED`.** **6. Initial.** `RAISED`. **7. States.** the 5 (registry §4/M7). **8. Terminal.** `RESOLVED_BY_RULE, RESOLVED_BY_HUMAN`. **9. Non-terminal human-owned.** `OPEN, ESCALATED`. **10. Recoverable.** `RAISED`. **11. Failure.** none. **12. Expiry.** ### **NEVER — it ages and escalates.** **13. Unknown-outcome.** ### **none — a Conflict is NOT `unknown`; we have too much info, disagreeing (I8).**

## 14. Transition table

| ID | From → To | Trig | Preconditions / guards | Writes | Event | Test |
|---|---|---|---|---|---|---|
| **CF-1** | — → `RAISED` | S\|X | `kind ∈ {SYSTEM_VS_SYSTEM, CLAIM_VS_CLAIM, CLAIM_VS_OBSERVATION, INFERRER_VS_OWNER, READBACK_VS_APPROVED, RULE_VS_RULE}`; ### **a human `owner_id` assigned at creation**; ### **the field becomes `conflicting` (GR-10)** | `entity_ref, field, parties[], owner_id` | `ConflictRaised{kind}` | `test_cf_raise_freezes_field_and_assigns_owner` |
| **CF-2** | `RAISED` → `OPEN` | H\|S | acknowledged | — | `ConflictOpened` | `test_cf_open` |
| **CF-3** | `OPEN` → `RESOLVED_BY_RULE` | S | ### **a REGISTERED, versioned, deterministic rule (rule_id) applies — never recency, confidence, or a model** | `rule_id` | `ConflictResolved{rule_id}` | `test_cf_rule_resolution_requires_registered_rule_id` |
| **CF-4** | `OPEN` → `RESOLVED_BY_HUMAN` | H | ### **`decision_ref` valid (GR-14)** | `decision_ref` | `ConflictResolved{decision_ref}` | `test_cf_human_resolution_requires_decision_ref` |
| **CF-5** | `OPEN` → `ESCALATED` | T | `AgeThresholdCrossed` (durable timer) | — | `ConflictEscalated` | `test_cf_ages_to_escalated` |
| **CF-6** | `ESCALATED` → `{RESOLVED_BY_RULE,RESOLVED_BY_HUMAN}` | S\|H | as CF-3/CF-4, resolved by TARGET STATE (never positionally) | as those | `DELEGATES_TO:RESOLVED_BY_RULE=CF-3;RESOLVED_BY_HUMAN=CF-4` | `test_cf_escalated_resolves` |
| **CF-7** | `{RAISED,OPEN}` → *(more parties)* | S\|X | a new disagreeing party detected | append `parties[]` | `EVENT_REQUIRED:G2-OB-CF-7-PARTY-ATTACH-FACT-UNRECORDED` | `test_cf_new_party_attaches_not_new_conflict` |

## 15. Illegal transitions. ### **`AutoResolve` and ANY `TimerFired`-to-resolved → ILLEGAL** (a clock knows nothing about freight). Resolution with neither `rule_id` nor `decision_ref` → ILLEGAL. A model resolving → ILLEGAL (GR-7). A consequential action proceeding on a field with an OPEN conflict → ILLEGAL/blocked (GR-10). An ownerless Conflict → ILLEGAL (owner at creation). **16. Precedence.** ### **while OPEN, the field is `conflicting` and BLOCKS every consequential transition on the entity (GR-10)** — this precedence dominates all machines that read the field. **17. Concurrency.** `UNIQUE(tenant,entity_ref,field) WHERE state∈{RAISED,OPEN,ESCALATED}` — a second detection attaches a party (CF-7), not a new conflict. **18. Version-check.** n/a (the Conflict itself); it *causes* consequential blocks elsewhere. **19. Idempotency.** GR-4 + the partial unique index. **20. Retry.** n/a. **21. Replay.** GR-11.
## 22. Cancellation. only if the **underlying disagreement disappears** (a party retracts) — still an event, never silence. **23. Expiry.** never. **24. Reopening.** a resolved field that *later* attracts new conflicting evidence raises a **NEW** Conflict (CF-1) — the prior resolution stands in history (GR-12). **25. Correction.** a resolution may *cause* a downstream claim correction (M6 IB-7). **26. Supersession.** n/a. **27. Compensation.** if resolution reveals a wrong completed effect ⇒ M10. **28. Human-approval.** an open Conflict on a material field voids/blocks the M4 approval. **29. Policy.** ### **RULE_VS_RULE: two conflicting standing rules ⇒ this Conflict ⇒ a human resolves; Neyma never picks a winner (GR-15).** **30. Brake.** conflict detection continues under a brake.
## 31. Evidence/provenance. each `parties[]` entry carries its `provenance_class`; `INFERRER_VS_OWNER` records that one party is `OWNER_ASSERTED`. **32. Events.** CF-* per registry. **33. Consumes.** `ConflictDetected, Acknowledged, DeterministicRuleApplies, HumanResolved, AgeThresholdCrossed`. **34. Writes.** conflict row + field-condition + outbox. **35. Txn.** raise+freeze one commit. **36. Crash recovery.** an open Conflict persists; the frozen field stays blocking until resolved. **37. Timeout.** CF-5 escalation only (never resolution). **38. Observability.** ### **conflict count should be near zero; a persistent conflict is an operational problem in the business — surfacing it IS the product.** **39. Audit.** raise/open/escalate/resolve with parties and resolution basis. **40. Security.** ### **a Conflict is a security control — an injected competing claim yields a FROZEN entity + a human's attention, not control.** Resolution requires a registered rule_id or an authenticated human decision_ref.
## 41. Acceptance. (a) open Conflict blocks all consequential actions on the entity; (b) no timer/model resolves; (c) resolution needs a rule_id or decision_ref; (d) inferrer-vs-owner raises rather than overwrites; (e) two conflicting rules block, not auto-merge.
## 42. Adversarial. Traces 6 (conflicting observation), 7 (inferrer-vs-owner), 13 (correction).
## 43. Open validation. **V5** (registered conflict-resolution rules). Fail-closed: no rule ⇒ every conflict to a human. Not a block.
