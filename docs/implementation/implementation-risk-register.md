# Implementation Risk Register

*Each risk: the failure it produces · the phase it lives in · ### **the STRUCTURAL mitigation (a mechanism, not a promise)** · the detecting acceptance case · the owner.*
> ### **A mitigation that reads "we will be careful" or "the reviewer will notice" is NOT a mitigation. Where the honest mitigation is discipline rather than a mechanism, this register says so out loud (R-07, R-16).**

| ID | Risk | Failure it produces | Phase | ### Structural mitigation | Detected by | Owner |
|---|---|---|---|---|---|---|
| ### **R-01** | ### **a migration infers success for ambiguous historical effects** | ### **manufactured verified-success at scale, in ONE commit — the worst single act available in this plan** | P1/P2 | ### **the data-migration rule: ambiguous ⇒ `UNKNOWN_OUTCOME` + owner + the key held. The backfill has no code path that writes VERIFIED.** | `AC-REC-*`, the dry-run report | architect |
| ### **R-02** | ### **a second effect namespace appears** (legacy + canonical keys coexist) | ### **double-commit — the exact defect the plan exists to fix, re-created by the fix** | P1–P4 | ### **ONE ledger, ONE key namespace, from U2.3 onward; coexistence is permitted ONLY through the shared unique index (principle 5)** | `AC-SAFE-014`, `AC-RACE-001` | architect |
| **R-03** | a parallel write path survives P4 | an ungated live write | P4 | ### **U4.6 PHYSICAL DELETION + U4.9 the CI import gate — not a convention** | `AC-SEC-013`, `AC-ADPT-002` | tech lead |
| **R-04** | the checkpoint gets a bypass flag "for testing" | the kernel becomes optional | P3+ | ### **`CheckpointPassed` has no public constructor: the bypass FAILS TO COMPILE. No flag can undo a type.** | `AC-SAFE-002` (negative control) | architect |
| **R-05** | async work sneaks in before the claim CAS | a TOCTOU window ⇒ double effect | P3 | ### **the 7 steps in ONE transaction; the CAS is the last act** | the 105 `AC-CKPT-*`, 10k interleavings | architect |
| **R-06** | tenant scoping lands late | cross-tenant effect | P2 | ### **P2 precedes ALL effect enablement; `AC-SEC-001` fails the build until ALL 7 offending tables are fixed** *(errata: was 6/8)* | `AC-SEC-001` | architect |
| ### **R-07** | ### **the 6 live-write paths remain reachable UNTIL P4** | ### **an ungated live write TODAY** | ### **P0–P4 (NOW)** | ### **NONE that is structural. The honest mitigation is the runbook's one-writer discipline — OPERATOR DISCIPLINE, NOT A MECHANISM. This is the plan's largest standing risk and it is not solved by planning; it is solved by reaching P4.** | ### **nothing automated — that IS the risk** | ### **the owner, personally, until P4** |
| **R-08** | mass rename before behavior | ### **code that reads canonical and behaves legacy — defeats every reviewer** | P8 | ### **names follow behavior: each rename is the LAST commit of the phase that made it true; renames ship behavior-free** | diff-shape review | tech lead |
| **R-09** | the `lane` migration (310 sites) mis-splits the 3 meanings | policy applied to the wrong scope | P8 | ### **it is LAST, after typed policy exists, so each meaning has a real home** | `AC-CKPT-6-*` | architect |
| **R-10** | the `workflow_runs` split collapses back | obligation ≡ attempt ⇒ ### **pipeline completion read as loop closure** | P6 | ### **MERGE_FORBIDDEN in the data plan; `AC-FC-015` proves a completed pipeline leaves the Work Item OPEN** | `AC-FC-015` | architect |
| **R-11** | the simulator is written from the implementation | ### **a false pass: the test agrees with the bug** | P3/P4 | ### **the simulator lives with the SPEC (L-9)** | `AC-ADPT-000` | tech lead |
| **R-12** | replay emits effects | a replayed real-world action | P5 | ### **effects structurally unreachable in the replay sandbox** | `AC-EVT-011` | architect |
| **R-13** | `MODEL_EXTRACTED` gets promoted to fact | a model choosing a money amount | P7 | ### **R-P2 no laundering; `MODEL_INFERRED` never gates at ANY confidence** | `AC-SAFE-015/016` | architect |
| **R-14** | an adapter converts `UNKNOWN_OUTCOME`→`FAILED` | ### **a retry of an effect that already happened** | P4 | ### **adapters return structured evidence only; the machine decides** | `AC-ADPT-011/015` | tech lead |
| **R-15** | "the page loaded" treated as health | ### **`VERIFIED_FAILURE` asserted from a blind channel ⇒ a false negative ⇒ a double write** | P4 | ### **positive health detection; proof of absence requires a healthy channel** | `AC-ADPT-012` | tech lead |
| ### **R-16** | ### **the wedge (W6→W8) is wrong** | ### **P10–P14 build the wrong product — correctly** | P10 | ### **partial: Phases 0–9 are loop-agnostic BY CONSTRUCTION, so a rejection costs P10 only. But nothing in the architecture can tell us the wedge is wrong — ONLY THE DESIGN PARTNER CAN. Marked NEEDS DESIGN-PARTNER VALIDATION.** | ### **not testable — it is a market fact** | ### **the owner** |
| **R-17** | ### **the kernel is deferred "until after the pilot"** | ### **the pilot becomes the reason the kernel never lands, and the first live write goes out ungated** | any | ### **G4 is the wall: P10+ cannot start. Principle 13: no phase may depend on future work for a CURRENT safety guarantee.** | the gate | architect |
| **R-18** | a flag is added that bypasses rather than narrows | the kernel becomes advisory | any | ### **the flag table enumerates every flag; none disables the checkpoint/ledger/brake/tenancy/import-gate** | flag review at each gate | architect |
| **R-19** | scope creep: broad feature work before G4 | ### **breadth on an unsafe foundation** | P0–P9 | ### **the dependency spine + gate entry criteria; the roadmap admits NO ordering deviation** | gate entry | architect |
| **R-20** | ### **rollback is mistaken for compensation** | ### **an operator believes a flag-off "undid" a real external effect** | P12+ | ### **stated per phase: reversing a real effect is a NEW, separately-gated, separately-approved compensating effect with its OWN commit key. A flag never un-writes the world.** | `AC-REC-*` | owner |
| ### **R-21** | ### **a standard GitHub PR merge lands a merge commit on top of a finalized pair** | ### **the status authority goes stale beyond every legal state — receipts no longer bind to `HEAD^`, and the next unit begins on a topology the repository forbids (this ALREADY happened: `main` at `152574e`)** | ### **any unit at integration** | ### **fast-forward-only integration ([`integration-topology-procedure.md`](integration-topology-procedure.md)): if `origin/main` is not already an ancestor it is MERGED INTO the unit branch as ONE content commit whose FIRST parent is the branch's metadata commit — so the first-parent chain keeps binding — the finalizer's metadata commit goes directly on top with exactly one parent, and `main` is advanced by `push <branch>:main`, which creates no commit and rewrites nothing. ### **NEVER rebase a branch that already carries a metadata commit:** it rewrites the recorded SHA and no finalizer can then repair it. ### **The topology guard is NOT a complete control for the merge button.** It catches the button's usual shape — feature merged INTO base, which puts the certified pair on the second-parent side so the chain does not bind. It does **not** catch a button-created merge whose FIRST parent happens to be the branch's metadata commit: after `R-21b` that is topologically **indistinguishable** from a legitimate integrating merge and is accepted. For that class the control is the **repository configuration — GitHub must allow ONLY rebase merging** — backed by independent review and a real finalizer run on every content commit, not by the topology guard. **`Require linear history` must stay OFF**, or it would reject the integrating merge integration depends on.** | `test_integration_topology.py`, `test_status_reality.py`, Product Driver `protocol` (advisory; PD-02) | ### **the owner** (the push is a founder-authorized act) |
| ### **R-21b** | ### **the integration procedure itself was unexecutable, and its rehearsal never looked at the state where it failed** | ### **the topology guard expressed the invariant as a PARENT COUNT, rejecting a merge at `HEAD^` — which in `FINALIZED` is the CONTENT commit, a case §2 never constrained. Since integrating a diverged `origin/main` necessarily puts a merge on the first-parent chain, the recorded anchor was TRAPPED BELOW it: every position where `repo_state()` bound was rejected on parent count, every position accepted was stale, and `finalize_status.py` validates before it writes — so integration was unreachable and the documented Step 4 could never run** | ### **any unit at integration; discovered at P6 with the branch 48 ahead / 5 behind `origin/main`** | ### **the predicate now asserts the two properties that matter — the first-parent chain binds to the recorded pair, and the status-metadata commit has exactly one parent — and the hermetic scenarios evaluate them AT THE FINALIZER-TIME STATE against the real `repo_state()`, not only on the final pair.** ### **THE RELAXATION IS CONFINED TO ONE THING: the CONTENT commit is no longer required to have exactly ONE parent.** Its parent count is still read and still bounded — at **TWO**, the branch and `origin/main`. Retained without change: a merge used as the **metadata** commit is **still REJECTED**; an octopus metadata commit is **still REJECTED**; a chain that fails to bind the recorded pair — the usual merge-button shape — is **still REJECTED**; stacked unfinalized content commits are **still REJECTED**; and an **octopus CONTENT commit** — three or more parents, smuggling a lineage that `git show` renders as an empty patch — is **REJECTED** by the two-parent bound. Each is covered by a test that fails when its rule is removed. ### **What may now be accepted is narrow and conditional:** a **two-parent** merge used as the **content** commit, and only where the certified chain binds, the state is `PRODUCING`, and a finalizer is owed on it — the first-parent proviso being enforced by `repo_state()` going stale or invalid at finalizer time, not by a parent-shape check at rest. ### **The cost is stated rather than hidden:** a button-created merge that preserves first-parentage is now indistinguishable from a legitimate integration, so the control for that class is the allow-only-rebase-merging repository setting, not this guard (see R-21). ### **No history was rewritten and `main` was not touched.** Integration remains fast-forward-only per [`integration-topology-procedure.md`](integration-topology-procedure.md) §5.** | `test_integration_topology.py` (20 nodes, incl. the finalizer-time assertion, the merge-as-metadata forgery, the octopus-content-commit rejection and the anchor-reachability regression) | ### **the owner** (the push is a founder-authorized act) |
