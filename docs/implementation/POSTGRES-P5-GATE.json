{
  "gate": "postgres_p5",
  "schema_version": "pg-p5-1",
  "passed": true,
  "executed_at": "2026-08-14T05:15:59.000Z",
  "server_version": "PostgreSQL 16.15 (Homebrew) on aarch64-apple-darwin24.6.0",
  "migration_steps_first_run": 26,
  "migration_steps_on_replay": 0,
  "invariant_results": {
    "outbox envelope immutable": "REFUSED",
    "outbox no delete": "REFUSED",
    "outbox strict version owner": "REFUSED",
    "inbox append-only update": "REFUSED",
    "inbox append-only delete": "REFUSED",
    "timer schedule immutable": "REFUSED",
    "timer no delete": "REFUSED",
    "POSITIVE CONTROL: outbox delivery bookkeeping may move": "ACCEPTED",
    "POSITIVE CONTROL: timer delivery bookkeeping may move": "ACCEPTED",
    "timer terminal": "REFUSED",
    "atomic emit persists": "PASS",
    "rollback leaves NO event": "PASS",
    "duplicate emission refused": "PASS",
    "strict-order trigger classified correctly": "PASS",
    "relay publishes and marks": "PASS",
    "duplicate relay workers deliver once": "PASS",
    "inbox applies once, redelivery is a no-op": "PASS",
    "concurrent emitters allocate distinct sequences": "PASS",
    "timer not due before its deadline": "PASS",
    "timer survives a restart": "PASS",
    "timer fires after restart": "PASS",
    "a fired timer does not fire again": "PASS",
    "an unarmed connection refuses BEGIN IMMEDIATE": "PASS",
    "a leased outbox row cannot be claimed by a second CONNECTION": "PASS",
    "a LAPSED lease is reclaimable by another connection": "PASS",
    "a caught integrity error does not poison the transaction": "PASS",
    "tenant isolation holds": "PASS"
  },
  "findings": [],
  "_what_this_proves": "The P5 durable surface applies to a real PostgreSQL, replaying the migration is a genuine no-op, tenant-first keys are preserved, all eight invariants REFUSE (with a positive control proving the tables are not merely read-only), and the SQLite and PostgreSQL catalogs agree column-for-column and key-for-key.",
  "_runtime_proved": "The ACTUAL runtime classes - TransactionalOutbox, OutboxRelay, DedupInbox, DurableTimers, TimerRelay - were driven against this PostgreSQL through persistence.PostgresConnection: atomic emit, rollback leaving nothing, duplicate emission refused, the strict-ordering TRIGGER classified as StrictOrderViolation rather than misreported as a duplicate, relay claim/publish/bookkeeping, duplicate relay workers delivering exactly once, inbox dedup with the handler called once, EIGHT CONCURRENT emitters on real threads allocating distinct sequences, timer schedule/restart-recovery/fire/terminal, and tenant isolation.",
  "_what_this_does_NOT_prove": "The P2/P3 surface - the tenant-owned state tables and the checkpoint kernel's - is NOT ported: those belong to the phases that own them, and ADR-016 assigns deployment and environments to P11. No pooling, no failover, no backup/PITR - ADR-016 assigns those to P11/P12."
}
